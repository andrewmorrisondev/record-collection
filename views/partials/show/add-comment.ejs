<% if (user) { %>
  <div class="add-comment">
    <form action="/records/<%= record._id %>/comments" method="POST">
      <textarea
      cols="30"
      rows="5" 
      type="text"
      name="body" 
      autocomplete="off"
      ></textarea>
      <button type="submit">Comment</button>
    </form>
  </div>
<% } %>