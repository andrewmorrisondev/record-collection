<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<% if (user) { %>
  <h1><%= title %></h1>
  <div class="profile-cards">
    <% profiles.forEach(profile => { %>
      <div class="profile-card">
        <a href="/profiles/<%= profile._id %>"><%= profile.name %></a>
      </div>
    <% }) %>
  </div>
<% } %> 

<%- include('../partials/footer') %>